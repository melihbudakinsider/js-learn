const array = ['Araba', 'Ev', 'Saat'];
const copiedArray = array.slice();

/**
 * slice bir diziyi startIndex ve endIndex parametrelerine göre yeni bir diziye kopyalar
 * eğer boş bir parametre gönderiliyorsa slice tüm diziyi kopyalayarak yeni bir dizi oluşturur.
 */
console.log(copiedArray);