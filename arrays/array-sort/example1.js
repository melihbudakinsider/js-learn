const items = [10, 30, 20];

items.sort()

/**
 * Dizi elemanlarını sıralamak istediğimizde .sort() metodunu kullanırız. Böylece dizi içerisindeki elemanları binary
 * olarak sort edilir. Bu işlem dizinin yapısını değiştirdiği için dikkatli kullanılmalıdır. Yeni bir kopya üretmez.
 */
console.log(items);