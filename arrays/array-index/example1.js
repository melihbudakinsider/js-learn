const array = ["Honda", "BMW", "Volkswagen"];

/**
 * indexOf dizi içerisinde parametre olarak geçilen değerin varlığını kontrol eder,
 * dizi içerisinde olması durumunda elemanın dizideki index değerini döndürür.
 */
console.log(array.indexOf("BMW"));